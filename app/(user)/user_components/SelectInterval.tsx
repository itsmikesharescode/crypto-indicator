import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SelectInterval = () => {
  return (
    <View className="w-full">
      <Text>SelectInterval</Text>
    </View>
  );
};

export default SelectInterval;

const styles = StyleSheet.create({});
